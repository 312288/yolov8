#YOLOV5
没有论文
详细内容见语雀
#1.1
结构基本没变，还是主干，颈部，检测头，模型架构
输出层:实现分类定位一体 ==>主要还是目标检测

#2.1
Mosaic数据增强 目标检测+分类 中均可使用
1.多样化背景下模型的鲁棒性
2.通过同一张图像放置多个目标，提升小目标检测能力
3.增加训练模型的多样性，可以更可能应对不同的场景和变化
4.在目标检测的作用：
4张图片拼接在一起形成新的图片，重新计算目标的目标框，以适应拼接后新的坐标系

#2.2 自适应锚框计算
根据训练数据集大小，自动调整锚框大小，更好适应检测任务

#2.3 自适应图片缩放
统一为同一尺寸

#2.4 SiLU激活函数

#3模型结构
头 颈 检测头 分类结构

#3.1 头
Focus:减少层数，减少参数量，减少计算量，
原图拆分和原图卷积池化
后叠加融合
focus={拆分==>}
                ==>concat==>conv
      {拆分==>}

#C3模块
CBS=COV+BN+SiLU
ResUnit-T=->CBS+CBS->add-->
            ↓----------↑
ResUnit-F=->CBS+CBS->

c3_1_x=->CBS->esUnit-T--↓
        ↓-------------->Concat-->CBS

c3_2_x=->CBS->esUnit-F--↓
        ↓-------------->Concat-->CBS

3.2 主干


#3.2.1 SPPF
特征大小和通道数都没变，只是使得特征融合在一起了

#颈部
上下采样结合，CBS+C3模块
